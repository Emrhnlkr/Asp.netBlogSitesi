@model IEnumerable<MvcBlog.Models.Kategori>

@{ 
    Layout = null;  /*Burada bu sayfa kendine bir layout arıyor ve bunu demessek sıkıntı çıkıyor*/
}
<h4>Kategoriler</h4>
<div class="row">
    <div class="col-lg-6">
        @foreach (var item in Model)
        {
            <ul class="list-unstyled">
                <li>
                    <a href="~/Home/KategoriMakale/@item.KategoriId">@item.KategoriAd (@item.Makales.Count())</a> @*En sondaki count makale sayısını veriyor bize ve view de görüntüleniyor (Kategorilerde kaç adet başlık altında kategorş olduğunu gösteriyor)*@
                </li>
            </ul>
        }
        
        </div>
    </div>


        @*<p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.KategoriAd)
                    </th>
                    <th></th>
                </tr>

            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.KategoriAd)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.KategoriId }) |
                        @Html.ActionLink("Details", "Details", new { id=item.KategoriId }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.KategoriId })
                    </td>
                </tr>
            }

            </table>*@
